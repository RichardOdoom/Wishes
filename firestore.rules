
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and create access to the wishes collection.
    // No one can update or delete wishes after they have been created.
    match /wishes/{wishId} {
      allow read, create: if true;
      allow update, delete: if false;
    }
  }
}
